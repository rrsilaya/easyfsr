/**
 * Export your global components here so that when
 * needed to be imported, you just have to supply the
 * index file as directory.
 */

export { default as HeaderConfig } from './headerconfig/HeaderConfig';
export { default as Sidebar } from './navigation/Sidebar';
